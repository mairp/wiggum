# Tasks: Add audit export

## 1. Domain contract

- [ ] 1.1 Add the export requirement to `openspec/specs/audit/spec.md`.
- [ ] 1.2 Add scenarios for an empty audit log and a populated audit log.

## 2. Implementation

- [ ] 2.1 Implement the exporter in `src/audit/export.py`.
- [ ] 2.2 Add focused tests in `tests/test_audit_export.py`.

## 3. Verification

- [ ] 3.1 Run the audit export test suite and record its result.
